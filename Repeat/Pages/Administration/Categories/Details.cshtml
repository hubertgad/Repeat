@page
@model Repeat.Pages.Administration.Categories.DetailsModel

@{
    ViewData["Title"] = "Details";
    Layout = "~/Pages/Administration/_Layout.cshtml";
}

<h1>Details</h1>

<div>
    <h4>Category</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Category.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Category.Name)
        </dd>
    </dl>
</div>
<div>
    @if (Model.Category.OwnerID == Model.userId)
    {
        <a asp-page="./Edit" asp-route-id="@Model.Category.ID" 
            class="btn btn-warning">Edit</a> <a>|</a>
    }
    <a asp-page="Index" class="btn btn-dark">Back to List</a>
</div>