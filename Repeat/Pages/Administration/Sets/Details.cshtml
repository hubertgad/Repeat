@page
@model Repeat.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Set</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Set.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Set.Name)
        </dd>
    </dl>
</div>
<div>
    <h6>Questions in this set:</h6>
    <hr />
    <dl class="row">
        @for (int i = 0; i < Model.Questions.Count(); i++)
        {
            <dt class="col-sm-3">
                <label>Question @(i + 1):</label>
            </dt>
            <dd class="col-sm-9">
                <label>@Model.Questions[i].QuestionText</label>
            </dd>
        }
    </dl>
</div>
<div>
    @{ if (Model.Set.OwnerID == Model.CurrentUserID)
        {
            <a asp-page="./Edit" asp-route-id="@Model.Set.ID">Edit</a> <a>|</a>
        }
    }
    <a asp-page="./Index">Back to List</a>
</div>